---
import Layout from "../layouts/layoutMain.astro";
import Contact from "../components/contact";
import ImageHeader from "../components/imageHeader";
import { services } from "../components/resources";
---

<Layout title="Services">
  <ImageHeader
    title="Our Services"
    subtitle="Learn How We Help You"
    backgroundImage="/images/zaworld.gif"
  />
  <section class="py-20 px-4 max-w-7xl mx-auto text-center">
    <h2 class="text-5xl font-bold mb-6">Hello Clean!</h2>
    <p class="text-lg text-gray-600 max-w-2xl mx-auto">
      Our services are designed to meet your needs with precision and care. We
      strive to deliver exceptional quality, ensuring your satisfaction every
      step of the way. Discover how our solutions stand apart:
    </p>

    <ul
      class="grid sm:grid-cols-1 md:grid-cols-2 gap-6 text-gray-700 max-w-5xl mx-auto mt-8 text-left"
    >
      <li class="flex items-start gap-4">
        <div>
          <strong>Fast Turnaround:</strong> We respond quickly and clean efficiently,
          minimizing disruption to your schedule.
        </div>
      </li>
      <li class="flex items-start gap-4">
        <div>
          <strong>Flexible Scheduling:</strong> We work around your hours to avoid
          interference with your business or routine.
        </div>
      </li>
      <li class="flex items-start gap-4">
        <div>
          <strong>Professional Equipment:</strong> We use industrial-grade pressure washing
          equipment.
        </div>
      </li>
      <li class="flex items-start gap-4">
        <div>
          <strong>Clear Communication:</strong> From quote to completion, we keep
          you in the loop and informed every step of the way.
        </div>
      </li>
    </ul>
  </section>

  <section class="py-20 px-4 max-w-7xl mx-auto text-center">
    <h2 class="text-5xl font-bold mb-6">Pressure Washing vs. Soft Washing</h2>
    <p class="text-lg text-gray-600 max-w-2xl mx-auto mb-8">
      Understanding the difference between pressure washing and soft washing is key to choosing the right cleaning method for your needs.
    </p>
    <div class="grid sm:grid-cols-1 md:grid-cols-2 gap-8 text-left">
      <div class="bg-white rounded-lg shadow-md p-6">
        <h3 class="text-2xl font-semibold mb-4">Pressure Washing</h3>
        <p class="text-gray-700">
          Pressure washing uses high-pressure water to remove dirt, grime, and stains from hard surfaces like concrete, brick, and stone. It is ideal for tough cleaning jobs but may not be suitable for delicate surfaces.
        </p>
      </div>
      <div class="bg-white rounded-lg shadow-md p-6">
        <h3 class="text-2xl font-semibold mb-4">Soft Washing</h3>
        <p class="text-gray-700">
          Soft washing uses low-pressure water combined with specialized cleaning solutions to gently clean surfaces like roofs, siding, and fences. It is perfect for removing mold, algae, and mildew without causing damage.
        </p>
      </div>
    </div>
  </section>

  <h2 class="text-5xl font-bold mb-6 text-center">Learn more about our services</h2>
  <section
    class="grid gap-8 sm:grid-cols-2 lg:grid-cols-3 max-w-7xl mx-auto px-4"
  >
    {
      services.map((item) => (
        <a
          href={`/services/${item.id}`}
          class="block transform hover:scale-105 transition-transform duration-300"
        >
          <div class="bg-white rounded-lg shadow-md hover:shadow-lg p-6 text-center">
            <img
              src={item.photo}
              alt={item.name}
              class="w-full h-48 object-cover rounded-md mb-4"
            />
            <h3 class="text-xl font-semibold mb-2">{item.name}</h3>
            <p class="text-gray-700 mb-4">{item.info}</p>
            <div class="flex flex-wrap justify-center gap-2">
              {item.tags.map((tag) => (
                <span class="bg-blue-100 text-blue-800 px-3 py-1 rounded-full text-sm">
                  {tag}
                </span>
              ))}
            </div>
          </div>
        </a>
      ))
    }
  </section>

  <Contact />
</Layout>
